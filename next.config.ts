import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Next.js 16 App Router automatically handles file uploads
  // Increase if needed for larger files
};

export default nextConfig;
